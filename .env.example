# --- Supabase (Postgres + Auth + Storage) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. NEVER expose this to the client or commit a real value.
SUPABASE_SERVICE_ROLE_KEY=

# --- App ---
APP_URL=http://localhost:3000

# --- AI provider (abstracted behind AIProvider — see lib/domain, Phase 4+) ---
AI_PROVIDER=
AI_API_KEY=

# --- Image generation provider (abstracted behind ImageGenerationProvider, Phase 5+) ---
IMAGE_PROVIDER=
IMAGE_API_KEY=

# --- Payments (Razorpay first implementation per SPEC.md §5, Phase 9+) ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# --- Observability (Phase 10+) ---
SENTRY_DSN=
ANALYTICS_KEY=
